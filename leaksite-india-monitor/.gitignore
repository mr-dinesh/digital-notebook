raw/
*.duckdb
*.duckdb.wal
victims_export.csv
__pycache__/
venv/
logs/
.run_weekly.lock

# review_queue.csv and categories.csv are NOT committed: they name real organisations
# against unverified attacker claims, and this repo is public. They remain the two
# artifacts here that cannot be regenerated, so back them up out-of-band. The code is
# shareable; the victim data stays local.
review_queue.csv
review_queue.csv.*
categories.csv
categories.csv.*
